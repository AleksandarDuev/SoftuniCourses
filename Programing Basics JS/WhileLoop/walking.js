function walking(input) {
    let index = 0;
    let totalSteps = 0;
    let command = input[index];
    while (command !== "") {
        let steps = input[index];
        index++;
        console.log(steps);
         index++;
         break;
    }


}


walking(["1000",
    "1500",
    "2000",
    "6500"])
