function solve(order) {
    const result = {};
result[model]= order.model;
result[engine] = {order[power], }
}

solve({
  model: "VW Golf II",
  power: 90,
  color: "blue",
  carriage: "hatchback",
  wheelsize: 14,
});
